import Image from "next/image";

const Location = () => {
  return (
    <div className="bg-map h-[50rem] w-full bg-center bg-cover gradient-bottom flex items-end justify-center">
  
    </div>
  );
};

export default Location;
